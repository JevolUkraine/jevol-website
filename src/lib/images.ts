// Shared image paths that are reused across multiple pages.
//
// Set this to the real Supabase Storage path once the photo is uploaded to
// the "images" bucket, e.g. "site/factory-floor.webp". Leave it `null` to
// render the contact section without a photo (single-column, no gray box).
export const CONTACT_PHOTO_PATH: string | null = null;
